# Dev loop — how to run it

> **Working end to end** on Blender 5.0: real active-3D-view extraction → `.glb`
> bundle → Blender pipeline → PNG, plus an interactive Open Model session. The
> commands below are the real loop.

## Revit side (pyRevit)

1. The repo root **is** the extension (`.tab` at root). pyRevit finds extensions by
   the `.extension` folder suffix, so clone into a `Blendit.extension` folder
   (`git clone <url> Blendit.extension`), then in pyRevit's **Settings → Custom
   Extension Directories** add that folder's **parent**.
2. **Reload** pyRevit. A `Blendit` tab with a `Render` panel appears
   (Load Model · Open Model · Render Loaded Model · Mode · Quality · Resolution ·
   Engine · Open Renders · Settings).
3. Open a model, activate a **3D view**, then **Load Model** (extract it into the
   cache - the one slow step, with a progress bar). Then **Open Model** to view it
   in interactive Blender, or **Render Loaded Model** for a headless render as-is.
   Re-run **Load Model** after you change the model. Open / Render do nothing until
   a model is loaded (they tell you to Load first).

### How the Revit side reaches `contract/`

The exporter does `from contract.transport import ...`, but `contract/` lives at
the repo root — outside the extension's `lib/` (which pyRevit auto-adds to
`sys.path`). **Resolved:** `lib/bir_bootstrap.py` prepends the repo root (and
`lib/`) to `sys.path`, and the pushbutton calls `bir_bootstrap.ensure_paths()`
first — single source of truth for the contract, no vendoring. Integration
detail, **not** a contract change.

`bir_bootstrap` also resolves `blender.exe` (env `BLENDIT_BLENDER_EXE` → common
install paths → PATH) and the headless render-script path.

## Blender side (headless render)

The Revit button launches Blender headless per render — a fresh process each
time, which also satisfies the `bpy` "cannot reload" constraint:

```
blender.exe --background --python blender/headless/render.py -- ^
    --bundle "<scene_spec.json or bundle dir>" ^
    --out    "<output .png>" ^
    [--engine CYCLES|EEVEE] [--mode MODE] [--samples N] [--camera persp|ortho]
```

`MODE`: realistic, white, shadow, specular, linework, pen, sketch, cel.

`render.py` parses argv **after `--`**, resolves paths to absolute (Blender
relativizes `render.filepath` against its own base, not the CWD), picks the
importer by `spec["geometry"]["transport"]`, loads the bundle, runs the pipeline
(`look → camera → world → preset → engine`), and writes the PNG. CLI flags
override the SceneSpec so one bundle re-renders in any mode/engine.

Generate the fixture bundle first (no Revit needed):
`python tests/fixtures/build_fixture.py`. Resolve `blender.exe` from
`BLENDIT_BLENDER_EXE` if it isn't auto-detected.

## Tests (no Revit required)

The contract + pipeline are kept honest independently of Revit:

```
# with the pinned bpy wheel matching the target Blender's CPython
pytest tests/
```

`tests/fixtures/` holds the bundle (`scene_spec.json` + `scene.glb`: a box + a
glass pane, covering opaque + transparent paths), generated by
`build_fixture.py`. `tests/test_pipeline.py` loads it via the importer, runs the
pipeline for each render mode, and asserts a non-trivial PNG. Under plain CPython
the tests skip (no `bpy`).

**Quick check without pytest** (uses your installed Blender directly):

```
blender --background --python tests/smoke_render.py   # renders the 5 Cycles-safe modes
```

Pin the exact CI `bpy` wheel in [README.md](../README.md) once chosen. Verified
working on Blender 5.0.

## Open Model (interactive)

The **Open Model** button launches Blender **not** headless (`blender --python
blender/interactive/live.py -- --bundle … --blend … --mode white`), opening the
loaded model in a stripped "Fly" review session (EEVEE, live Look + per-mode
sliders, WYSIWYG camera framing, Enter to capture). It opens the prepared `.blend`
cache when present, so repeat opens skip the re-import. If no model is loaded it
tells you to **Load Model** first.

**Still to come (true live link):** a persistent Blender + websocket (pyRevit
Routes) so Revit edits stream through without re-running **Load Model**. Same
`Importer`; a new push transport.
